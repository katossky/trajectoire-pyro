[project]
name = "trajectoire-pyro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.0",
    "pyro-ppl>=1.9.1",
    "torch>=2.7.1",
]
